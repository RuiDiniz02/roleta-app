// This service worker is intentionally left blank for this simple example.
// It's required for the app to be considered a PWA.
self.addEventListener('fetch', (event) => {
    // For a simple app, we don't need to handle fetch events.
    // The browser will handle network requests as usual.
  });